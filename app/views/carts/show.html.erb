<% if notice %>
<p id="notice"><%= notice %></p> <% end %>
<h1>Your little Cart of Horros</h1> 
<ul>
<% @cart.line_items.each do |item| %> 
	<li><%= image_tag(item.product.image_url, :size => "50x50")%> <%= item.product.title %></li>
<% end %> 
</ul>

